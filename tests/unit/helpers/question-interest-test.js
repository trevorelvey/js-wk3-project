import { questionInterest } from 'js-messageboard/helpers/question-interest';
import { module, test } from 'qunit';

module('Unit | Helper | question interest');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionInterest([42]);
  assert.ok(result);
});
